import { Middleware } from 'koa';
import * as Router from 'koa-router';
export declare enum HttpMethod {
    HEAD = 0,
    OPTIONS = 1,
    GET = 2,
    PUT = 3,
    PATCH = 4,
    POST = 5,
    DELETE = 6,
    ALL = 7
}
export declare function route(path: string, method?: HttpMethod, ...middleware: Array<Middleware>): (target: any, key?: string | symbol | undefined, descriptor?: any) => void;
/**
 * 加载所有controller文件
 * @param controllersDir 陈放所有route的文件夹路径
 * @param extension 只导入对应后缀的文件作为route，默认导入所有的.js文件
 */
export declare function load(controllersDir: string, extension?: string): Router;
